# ── Streamlit Cloud Frontend Requirements ──────────────────────────────────
# Lightweight, clean dependencies for app.py (installs in ~5s with 0 errors)
streamlit>=1.35.0
requests>=2.31.0
reportlab>=4.0.0
pypdf>=4.0.0
